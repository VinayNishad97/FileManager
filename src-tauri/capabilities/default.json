{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "fs:allow-read-dir",
    "fs:allow-read-file",
    "fs:allow-app-meta"
  ]
}
